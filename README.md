# Purpose

This is a Windows utility application that displays a fullscreen black window behind the League of Legends game window to hide taskbars and other distractions. It uses Python with pywin32 for Windows API calls and tkinter for the GUI.
